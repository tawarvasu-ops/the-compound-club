export default function Footer() {
  return (
    <footer className="max-w-3xl mx-auto px-6 md:px-10 py-12 mt-12 border-t border-line">
      <p className="font-mono text-xs text-mist tracking-wide">
        Fieldnotes — a running record, updated when there's something worth
        dating.
      </p>
    </footer>
  );
}
